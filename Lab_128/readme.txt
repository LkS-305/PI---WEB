Após entrar na pasta corretamente, digite os seguintes comando:
    npm install express
    npm install nodemailer
    npm install body-parser
    npm install dotenv

Para subir o servidor, digite o seguinte código no terminal:
    node server.js


Para configurar o email que envia os formulários, preencha o documento '.env' com as informações comentadas no canva